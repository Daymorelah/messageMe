language: node_js
node_js:
  - "node"
branches:
  only:
    - test
notifications:
  email:
    on_success: change
    on_failure: change
before_script:
  - psql -U postgres -w -c 'create waleDB' 
services:
  -postgresql
script:
  - npm test
env:
  - NODE_ENV=test
